git:
  autoFetch: false
  paging:
    colorArg: always
    pager: delta --dark --paging=never
gui:
  nerdFontsVersion: "3"
  border: "rounded"
  theme:
    selectedLineBgColor:
      - underline
    selectedRangeBgColor:
      - reverse

